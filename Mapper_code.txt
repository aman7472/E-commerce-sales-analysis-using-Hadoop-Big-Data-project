!/usr/bin/env python
import sys

for line in sys.stdin:
    if line.startswith("Order Date"):
        continue

    data = line.strip().split(',')

    try:
        print "%s\t%s,%s" % (data[2], data[5], data[6])
    except:
	pass
